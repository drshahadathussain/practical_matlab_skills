function BlueWhaleData = importWhaleData(filename, dataLines)
%IMPORTFILE Import data from a text file
%  BLUEWHALEDATA = IMPORTFILE(FILENAME) reads data from text file
%  FILENAME for the default selection.  Returns the data as a table.
%
%  BLUEWHALEDATA = IMPORTFILE(FILE, DATALINES) reads data for the
%  specified row interval(s) of text file FILENAME. Specify DATALINES as
%  a positive scalar integer or a N-by-2 array of positive scalar
%  integers for dis-contiguous row intervals.
%
%  Example:
%  BlueWhaleData = importfile("/Users/sh8454/Downloads/downloadsFolders/practical_matlab_skills/MATLAB Course Files/Section 3/BlueWhaleData.csv", [2, Inf]);
%
%  See also READTABLE.
%
% Auto-generated by MATLAB on 07-Jan-2026 12:22:54

%% Input handling

% If dataLines is not specified, define defaults
if nargin < 2
    dataLines = [2, Inf];
end

%% Set up the Import Options and import the data
opts = delimitedTextImportOptions("NumVariables", 8);

% Specify range and delimiter
opts.DataLines = dataLines;
opts.Delimiter = ",";

% Specify column names and types
opts.VariableNames = ["ID", "timestamp", "longitude", "latitude", "sensorQuality", "sensorMeasurements", "sensorType", "markedOutliers"];
opts.VariableTypes = ["categorical", "datetime", "double", "double", "categorical", "double", "categorical", "string"];

% Specify file level properties
opts.ExtraColumnsRule = "ignore";
opts.EmptyLineRule = "read";

% Specify variable properties
opts = setvaropts(opts, "markedOutliers", "WhitespaceRule", "preserve");
opts = setvaropts(opts, ["ID", "sensorQuality", "sensorType", "markedOutliers"], "EmptyFieldRule", "auto");
opts = setvaropts(opts, "timestamp", "InputFormat", "yyyy-MM-dd HH:mm:ss.SSS", "DatetimeFormat", "preserveinput");

% Import the data
BlueWhaleData = readtable(filename, opts);

end